name: Version of OS
on: 
  workflow_dispatch:
jobs:
  ubuntu:
    runs-on: ubuntu-latest
    steps:
      - run: ${{ runner }}
  macos:
    runs-on: macos-latest
    steps:
      - name: env
        run: env
  Linux:
    runs-on: ubuntu-latest
    steps:
      - name: env
        run: env
  Windows:
    runs-on: windows-latest
    steps:
      - name: env
        run: env
