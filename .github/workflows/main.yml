name: test

on: [ push, workflow_dispatch ]


jobs:
  test_api:
    runs-on: ubuntu-latest 
    permissions:
      contents: read
    steps:
      - name: Pull a release using REST API
        run: |
          touch text
          echo "soehting" > text
      - name: Cache
        uses: actions/cache@v3.0.1
        with:
          # A list of files, directories, and wildcard patterns to cache and restore
          path: ~/text
          # An explicit key for restoring and saving the cache
          key: vcpkg-${RUNNER_OS}-${GITHUB_BASE_REF}-kfjwdslkjfladjf
      - name: retreive
        uses: actions/cache@v3.0.1
        with:
          # A list of files, directories, and wildcard patterns to cache and restore
          path: text2
          key: vcpkg-${RUNNER_OS}-${GITHUB_BASE_REF}-klsdjfakldfj
          # An explicit key for restoring and saving the cache
          restore-keys: |
            vcpkg-${RUNNER_OS}-${GITHUB_BASE_REF}-klsdjfakldfj
            vcpkg-${RUNNER_OS}-${GITHUB_BASE_REF}-
      - name: test response
        run: |
          cat text text2
